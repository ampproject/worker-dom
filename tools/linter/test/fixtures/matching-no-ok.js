pageXOffset;
x = pageXOffset;
x = pageXOffset + 10;
x(pageXOffset);
window.pageXOffset;
btn.offsetWidth;
btn['offsetWidth'];
bth.getBoundingClientRect();
bth['getBoundingClientRect']();
getComputedStyle();
getComputedStyle(btn);
window.getComputedStyle(btn);
if (pageXOffset) {
}
function one(btn) {
  var { offsetWidth } = btn;
  offsetWidth;
}
function two(btn) {
  var { offsetWidth: x } = btn;
}
